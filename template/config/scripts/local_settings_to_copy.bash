if [ "$RMW_IMPLEMENTATION" == "rmw_cyclonedds_cpp" ]; then
	# export CYCLONEDDS_URI="$WS_PROJECT_REPO/config/DDS/cyclonedds_sample.xml"
	# If you are using plotjuggler in this system, setting CYCLONEDDS_URI can cause error in plotjuggler. Ignore this comment if not using plotjuggler.
fi