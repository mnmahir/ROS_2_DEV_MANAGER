bashrc_list=(
    "# $DEV_PROJECT_NAME Development Path"
    "alias wst='source $WS_DEV_INIT_PATH /$WS_DEV_MANAGER_DIR_NAME/$WS_PROJECT_REPO_DIR_NAME /ws_robot'"        # Format: (alias <alias>='source $WS_DEV_INIT_PATH <repo dir> <ws dir>'). <repo dir> is relative to WORKSPACE dir. <ws dir> is relative to repo dir.
)
